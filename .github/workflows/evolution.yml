name: Continuous Evolution Factory

on:
  schedule:
    - cron: '*/20 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  evolve:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Evolving Repository Branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Initialize Execution Environment
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install Acceleration Packages
        run: |
          python -m pip install --upgrade pip
          pip install numpy jax jaxlib pytest

      - name: Execute High-Throughput Multi-Generation Optimization Loop
        run: |
          rm -f context/automator_execution.log
          python hyper_evolution_harness.py

      - name: Run Token Compressor
        run: |
          python bundle_repo.py

      - name: Autonomous Code Graduation Push
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # UNBOUNDED TRACKING: Track the entire src/ tree so new modules or edits are captured
          git add src/ tasks.json repo_context_bundle.txt
          
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "auto(evolution): upgrade convergence thresholds and structural layout [skip ci]"
            echo "Healing branch history concurrency variations..."
            git pull --rebase origin ${{ github.ref_name }}
            git push origin HEAD:${{ github.ref_name }}
          fi
